;Edit this file to bind the pixel check to a hotkey (remove only the semicolons (;) after this line, and replace ONLY the zero with the in-game archnemesis hotkey)

;#If WinActive("ahk_group poe_window")

;$0::GoSub, Fallback

;#If